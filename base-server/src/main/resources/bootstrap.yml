spring:
  profiles:
    active: dev
---
spring:
  application:
    name: BaseServer
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8762
      fail-fast: true
grass:
  swagger:
    title: 傻逼